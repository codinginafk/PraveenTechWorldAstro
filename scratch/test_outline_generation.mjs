import Database from "better-sqlite3";
import { runOutlinePlanner } from "../scripts/mission_control.mjs"; // Wait, is it exported? Let's check.
// If it's not exported, let's write the query and call LLM directly.
