# Content intelligence handoff — 2026-08-26

## Status

**DRAFT_ONLY — human approval required.** No article was made live, committed, deployed, syndicated, or submitted for indexing.

Selected lane: complete and improve two existing draft candidates in the `windows-fixes` cluster. The single-post release rule remains active; choose at most one candidate for a later publication session.

## Search and trend evidence

The AnswerThePublic free report accepted `windows 11 bluetooth missing`, but returned zero keyword rows and then locked further searches behind its daily limit. It was not used as demand evidence.

Reddit and current search-result language were more useful. Bluetooth discussions repeatedly use `Bluetooth disappeared`, `Bluetooth option/toggle disappeared`, `Bluetooth missing from Settings`, and `Bluetooth missing from Device Manager`. WSL2 discussions use `WSL2 no internet`, `WSL2 cannot connect to the internet`, `WSL2 DNS not working`, and VPN/networking variants. Microsoft’s current support wording also uses “Bluetooth disappeared,” while Microsoft Learn documents WSL internet failures around DNS tunneling, VPNs, NAT, and mirrored networking.

The titles were changed to match those phrases without changing the article slugs:

- Bluetooth: `Bluetooth Disappeared on Windows 11? How to Get It Back`
- WSL2: `WSL2 No Internet on Windows 11? Fix DNS and VPN Issues`

## Work completed

| Candidate | Status | SEO/image work | Remaining gate |
|---|---|---|---|
| `windows-11-bluetooth-disappeared-not-working-fixes` | Draft | Human-search title, SEO title/social hook, full cover URL, descriptive alt text, official Microsoft citation, contextual links, FAQ and summary sections | Sentence variety/paragraph style and GEO data-density gate |
| `wsl2-internet-not-working-windows-11-dns-vpn-fixes` | Draft | Human-search title, SEO title/social hook, full cover URL, descriptive alt text, official Microsoft citation, contextual links, FAQ and summary sections | GEO data-density gate |

Both articles remain `draft: true` and therefore are excluded from the live content path.

## Image assets

Generated with the project’s Minimal Flat Editorial formula: one centered subject, one anomaly, off-white background, charcoal linework, one accent color, and no text or logos.

- `public/images/generated/windows-11-bluetooth-disappeared-not-working-fixes.png` — USB Bluetooth adapter with a broken blue wireless signal.
- `public/images/generated/wsl2-internet-not-working-windows-11-dns-vpn-fixes.png` — network connector with a broken crimson link.

## Validation

- `node src/scripts/lint-missing-images.mjs` — passed for all 138 articles after the URL-aware path check was added.
- `node src/scripts/lint-duplicate-titles.mjs` — passed.
- Title lengths — Bluetooth 55 characters / SEO title 48; WSL2 54 / SEO title 40.
- Targeted quality gates — Bluetooth scored 85/100 and WSL2 scored 95/100; both remain draft-only because the remaining gates were not silently bypassed.
- `astro check` — blocked by the installed dependency tree: inaccessible/missing files under `node_modules/.pnpm` (`aria-query`, `axobject-query`, and Astro dev-toolbar modules).
- Direct `astro build` — blocked by the same dependency-tree errors. The build command was run directly, not through `npm run build`, to avoid scheduled publishing side effects.
- `npm run check` — unavailable because the environment’s npm launcher points to a missing `npm-cli.js` path.

## Project-memory decision

Graphiti was evaluated as a possible context graph. It is a real open-source temporal knowledge-graph framework, but its official quick start requires Python 3.10+, a graph database such as Neo4j or FalkorDB, and an LLM/embedding provider. Its MCP server is documented as experimental. For this single-repository workflow, the lower-risk first step is the repository-owned handoff layer in `docs/project-memory/` plus `scripts/project-memory.mjs`; it works across harnesses without introducing another state database.

## Human approval queue

1. Review both drafts for firsthand claims and decide which one should be the next single live article.
2. Refresh GSC/Bing evidence before selecting the publication candidate.
3. Repair the local dependency tree in a separate task, then rerun `astro check` and the direct build.
4. Do not install Graphiti until the repository memory contract proves insufficient or we need cross-project relationship retrieval.
