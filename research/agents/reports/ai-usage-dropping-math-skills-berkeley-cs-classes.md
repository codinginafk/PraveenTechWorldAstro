# Content Audit Report

**URL:** https://www.praveentechworld.com/blog/ai-usage-dropping-math-skills-berkeley-cs-classes
**Date:** 2026-07-01 18:24

## Overview

| Metric | Value |
|--------|-------|
| AI Score | **70/100** |
| Readability | Fairly Easy (Flesch: 51.4) |
| Word Count | 1881 |
| Sentence Count | 129 |

## Statistical Breakdown

| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| Burstiness | 0.722 | >0.75 | ⚠️ | Low variance = robotic rhythm |
| Lexical Diversity | 0.349 | >0.45 | ⚠️ | Low diversity = repetitive vocabulary |
| AI Cliché Density | 0.0 | <2.0 | ✅ | High = flooded with AI catchphrases |
| Conjunctive Adverb Density | 0.0 | <4.0 | ✅ | High = robotic transition spam |
| Em Dash Density | 3.19 | <0.5 | ⚠️ | High = dramatic punctuation overuse |
| Sentence Variation Ratio | 0.388 | >0.4 | ⚠️ | Low = all sentences same length |

## Overall Critique

The article is well-structured and informative, with a good mix of technical details and explanations. However, it occasionally suffers from overly complex sentence structures and technical jargon that could be simplified for better readability. The use of em dashes is slightly overdone, which can disrupt the flow. The article excels in providing actionable insights and clear examples, but some sections could benefit from more concise language.

## Actionable Fixes (5 found)

### 1. 📝 Redundant Conjunction

**Original:**

> The chat interface was making them dependent. So they stopped using it. Instead, they built autonomous agents that run in sandboxes, plan their own tasks, execute Python code, store results in SQLite, and email the output.

**Rewrite:**

> The chat interface made them dependent, so they stopped using it and built autonomous agents. These agents run in sandboxes, plan tasks, execute Python code, store results in SQLite, and email the output.

### 2. 📝 Overly Technical

**Original:**

> It receives a high-level task, decomposes it into subtasks, executes each subtask via sandboxed Python, stores intermediate results in a local SQLite database, and emails the final output.

**Rewrite:**

> It takes a high-level task, breaks it down into smaller steps, runs each step in a secure Python environment, saves progress in a local database, and emails the final result.

### 3. 📝 Overuse Of Em Dash

**Original:**

> The agent runs without human intervention. The student reviews the output the same way they would review a colleague’s work — critically, with full context.

**Rewrite:**

> The agent runs without human intervention. The student reviews the output critically, just as they would with a colleague’s work, and with full context.

### 4. 📝 Repetitive

**Original:**

> The agent does not need to remember context between steps. It reads from the database. This is the key insight: the database replaces the chat context window.

**Rewrite:**

> The agent doesn’t rely on memory between steps; instead, it reads from the database. This key insight means the database replaces the chat context window.

### 5. 📝 Overuse Of Em Dash

**Original:**

> If the objective is vague — “explore this dataset and find interesting patterns” — the agent will produce generic output that misses the human insight a domain expert would catch.

**Rewrite:**

> For vague objectives like 'explore this dataset and find interesting patterns,' the agent produces generic output, lacking the human insight a domain expert would provide.

---
*Generated by Content Auditor*